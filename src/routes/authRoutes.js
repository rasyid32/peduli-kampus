const express = require('express');

const router = express.Router();

/**
 * File ini dikerjakan oleh Anggota 2 untuk endpoint register dan login.
 * Rencana endpoint:
 * POST /api/auth/register
 * POST /api/auth/login
 */

module.exports = router;
