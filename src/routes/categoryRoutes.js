const express = require('express');

const router = express.Router();

/**
 * File ini dikerjakan oleh Anggota 4 untuk CRUD kategori laporan.
 * Rencana endpoint:
 * GET /api/categories
 * POST /api/categories
 * PUT /api/categories/:id
 * DELETE /api/categories/:id
 */

module.exports = router;
